<div class="row justify-content-left mt-3">
  <div class="col col-md-6">
      <h1><%= t('.header') %></h1>
        <table class="table table-bordered">
          <thead>
            <tr>
              <th><%= t('.title') %></th>
              <th><%= t('.questions') %></th>
              <th><%= t('.actions') %></th>
            </tr>
          </thead>
          <tbody>
            <%= render @tests %>
          </tbody>
        </table>
  </div>
</div>
