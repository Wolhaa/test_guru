<h2>Test <%= @test.title %> (questions: <%= @test.questions.count %>)</h2>
<%= link_to 'New question', new_test_question_path(@test) %>
<table>
  <tr>
    <th>Question</th>
    <th colspan="3">Action</th>
  </tr>
  <%= render @questions %>
</table>
